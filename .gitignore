node_modules/
logs/
*.log

# iOS build artifacts (keep these out of git; they're large)
apps/*.app
apps/*.ipa
apps/*.zip

# WDIO / test output
results/
.wdio-vscode-service/
.DS_Store

# Secrets / credentials (e.g. BrowserStack username & access key)
.env
.env.*
!.env.example

# BrowserStack
# Local testing binary (downloaded at runtime) and its logs/tunnel artifacts
BrowserStackLocal
BrowserStackLocal.exe
browserstack.err
browserstack.log
local.log
